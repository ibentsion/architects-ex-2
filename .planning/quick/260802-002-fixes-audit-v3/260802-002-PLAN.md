---
task: Agent fixes (unfiltered retry, difficulty routing) + refgen v3 in bg + retrieval hit-rate audit (pdf vs md)
slug: fixes-audit-v3
date: 2026-08-02
mode: quick
---

# Quick Task 260802-002 — eval follow-ups

Four tracks from the 260801-004 failure analysis (user-selected):

## T1 — Agent engine fixes
Files: `rag/agent/engine.py`, `rag/classify.py`, `rag/types.py`,
`rag/config.py`, `configs/default.yaml`, tests
1. **Unfiltered retry**: when a category-filtered retrieval (prefetch or the
   loop's retrieve tool) gates to zero, retry the same query unfiltered;
   trace records `retried_unfiltered`. Attacks the ~24 empty-pool refusals.
2. **Difficulty-aware synthesis routing**: classifier additionally estimates
   `difficulty` (easy|medium|hard, default medium on parse failure). Fast
   synthesis model (new harness fields `fast_synthesis_model/max_tokens/
   extra_params`, default gpt-oss-120b low in default.yaml; None disables)
   for single-mode easy/medium questions; `generation.model` (DeepSeek) for
   hard OR multi OR needs_calculation OR dependent — per user rule
   "gpt-oss for easy/single, DeepSeek for hard/multi/calc" and the v1
   evidence (models tie on easy/medium, DeepSeek wins hard 6.44 vs 4.12).

## T2 — refgen v3 (~30 multi-source/calc), launched in background
Files: `refgen/{run,generate,prompts,schema}.py`
- `--profile v3`: 3 multi_source items per category (36 total), id prefix
  `v3-`, `--holdout` accepts multiple files (v1 + v2 pages/questions
  excluded), `strict_counts` shape check skipped for v3.
- Calc bias: v3 multi_source prompt prefers RULE-PLUS-FIGURE / numeric
  shapes where the answer requires combining or computing figures from the
  two pages (operands + result stated in the ground truth).
- Launch locally under nohup (LLM-only workload); DO NOT wait — output
  `reference_questions_v3.json` + report used by a later task.

## T3 — Retrieval hit-rate audit, pdf vs markdown
Files: `evalharness/retrieval_audit.py` (new), `cloud/retrieval_audit.sh`
- Per question × ground-truth source group, classify the best stage reached:
  `missing_from_index` (corpus/parse gap — no chunk for that file+page,
  read from the sparse index's chunk_ids.json), `not_retrieved` (indexed
  but absent from dense∪sparse top-k), `not_gated` (candidate, killed by
  rerank gate/top-n), `gated` (hit). File matching by path-stem + page so
  pdf and markdown corpora compare.
- Arms: {pdf,markdown} × {per_table,per_page} × {v1,v2} (skip gracefully if
  an index is absent on the node). Retrieval only — no generation cost.
- Run on the L40S node, upload JSON summaries + per-question detail to the
  artifacts dataset, download, analyze: corpus gaps vs ranking failures,
  pdf-vs-md deltas.

## T4 — Wrap-up
Analysis note in this dir; STATE.md; commits pushed (cloud jobs run
origin/main).
