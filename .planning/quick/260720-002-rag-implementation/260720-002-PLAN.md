---
task: 260720-002-rag-implementation
description: Implement the RAG ingestion + query pipelines per rag_plan.md (T1-T10)
date: 2026-07-20
plan_source: rag_plan.md (repo root — authoritative; this file is wave bookkeeping)
---

# Quick Task 260720-002: Execute rag_plan.md

The authoritative plan is `rag_plan.md` §1–§11; the task breakdown is §10 (T1–T10).
This file only records the wave grouping for sequential executor sessions (no worktree
isolation: executors share `venv/`, HF model caches, and `.env`).

| Wave | rag_plan.md tasks | Scope |
|---|---|---|
| E1 | T1 | Scaffold, types, config registry, YAML configs, fixtures, deps install |
| E2 | T2 + T3 | Docling parsing + parse cache + RTL canary gate; 3 chunkers |
| E3 | T4 + T5 | Stanza normalizer + token cache; TF/local embedders + embedding cache; dense/sparse indexes + manifest |
| E4 | T6 + T7 | Ingestion CLI; retrieval (RRF, rerank, gate) |
| E5 | T8 + T9 | Generation + citation validation; query CLI (single/interactive/batch) |
| E6 | T10 | E2E test (2-category subset), full-corpus ingest, dev-set eval vs baseline |

Conventions for all waves:
- Run everything with `venv/bin/python` / `venv/bin/pytest`.
- One commit per rag_plan task: `feat(quick-260720-002): T<n> <short>`.
- Never commit: `.planning/`, `cache/`, `rag_index/`, model downloads. Keep
  `.gitignore` current.
- `NEBIUS_API_KEY` comes from `.env` (python-dotenv).
- RAM budget ~4 GB free — load models lazily, one at a time where possible.
