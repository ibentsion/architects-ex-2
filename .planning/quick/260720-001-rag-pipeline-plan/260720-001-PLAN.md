---
phase: quick-260720-001-rag-pipeline-plan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [rag_plan.md]
autonomous: true
requirements: [QUICK-260720-001]
must_haves:
  truths:
    - "rag_plan.md exists at repo root and an engineer can implement both pipelines from it without re-doing research"
    - "Every LOCKED decision from 260720-001-CONTEXT.md appears in the plan (Qdrant local, bm25s, Stanza, LangChain, Docling, bge-m3, hybrid+RRF, bge-reranker-v2-m3, 3 chunking strategies, chunk metadata contract, category filtering, retrieval gate, citation-mandating prompts, YAML config, 2 CLIs, tests)"
    - "Research rationale (DoclingDocument-JSON-not-Markdown, RTL canary gate, no langchain-qdrant HYBRID, lemma+surface union, pinned versions) is captured inline"
  artifacts:
    - path: "rag_plan.md"
      provides: "Implementation-ready plan: architecture, stack table, repo layout, YAML config schema + 2 examples, ingestion & query stage specs, CLI specs, metadata/citation contract, testing plan, 10-task breakdown, risks"
      min_lines: 400
  key_links:
    - from: "rag_plan.md"
      to: "reference_questions.json"
      via: "metadata & citation contract (§8) matches any_of {file, page} ground-truth format"
      pattern: "any_of"
    - from: "rag_plan.md"
      to: "tf_client.py"
      via: "generation stage routes all LLM calls through tf_client.chat"
      pattern: "tf_client"
---

<objective>
Author `rag_plan.md` — the implementation-ready plan for the Hebrew RAG ingestion +
query pipelines over the Harel insurance corpus (Exercise 2, Stage 2).

Purpose: This quick task is PLAN-ONLY. The deliverable IS the plan document; a
follow-up task implements it (its task breakdown is rag_plan.md §10).
Output: /home/ido/git/architects-ex-2/rag_plan.md
</objective>

<context>
@.planning/quick/260720-001-rag-pipeline-plan/260720-001-CONTEXT.md
@.planning/quick/260720-001-rag-pipeline-plan/260720-001-RESEARCH.md
@exercise2_customer_support_agent.md
@contract.py
@tf_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author rag_plan.md</name>
  <files>rag_plan.md</files>
  <action>Write the implementation-ready RAG pipeline plan at repo root, honoring
  every LOCKED decision in CONTEXT.md and incorporating RESEARCH.md findings with
  inline rationale. Required sections: overview + ASCII architecture, stack table
  with pinned versions, repo layout (rag/ package), YAML config schema + default
  and swap examples, ingestion stage spec (discover/parse+cache+RTL-canary/chunk/
  normalize/embed/index/manifest), query stage spec (normalize/dual-search/RRF/
  rerank+gate/assembly/generation/citation-validation/response incl. fallback),
  CLI specs (ingest + query with interactive mode), metadata & citation contract
  (NFC, any_of validation), testing plan (per-phase units + E2E + evalharness
  hookup), 10-task implementation breakdown with parallelization notes, risks
  table.</action>
  <verify>rag_plan.md exists, 400-600 lines, contains all 11 required sections;
  spot-check that Qdrant local, bm25s, Stanza, Docling, bge-m3, bge-reranker-v2-m3,
  RRF, RTL canary, and the {file, page, category} metadata contract all appear.</verify>
  <done>rag_plan.md written; a follow-up execution task can implement directly
  from §10 without re-research. DELIVERED by this quick task.</done>
</task>

</tasks>

<verification>
- rag_plan.md present at repo root with all 11 constraint-mandated sections.
- No LOCKED decision from CONTEXT.md omitted or watered down; no deferred ideas added.
</verification>

<success_criteria>
The plan document is the complete, self-contained input for the follow-up
implementation task (rag_plan.md §10, tasks T1-T10).
</success_criteria>

<output>
rag_plan.md (repo root — user-facing deliverable). This bookkeeping plan records
the quick task; no SUMMARY beyond the orchestrator's normal quick-task flow.
</output>
