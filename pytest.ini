[pytest]
testpaths = tests
pythonpath = .
markers =
    slow: end-to-end tests, real Docling parses, model downloads
    llm: needs NEBIUS_API_KEY (live Token Factory calls)
# Quick loop (no marker filtering by default):
#   pytest -x -m "not slow and not llm"
